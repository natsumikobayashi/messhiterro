<% @post_images.each do |post_image| %> <!--コントローラから渡された@post_images変数ないから１つずつ取り出して、post_image変数へ格納-->
<div>
  <%= link_to post_image_path(post_image.id) do %>
   <%= image_tag post_image.get_image %> <!--image_tagを使い、ActiveStorageで設定したimageを表示。PostImageモデル内のメソッド呼び出し-->
  <% end %>
  <p>投稿ユーザー画像: <%= image_tag post_image.user.get_profile.(100,100) %></p>
  <P>ショップ名:<%= post_image.shop_name %></P>
  <p>説明:<%= post_image.caption %></p>
  <p>ユーザーネーム:<%= post_image.user.name %></p>
</div>
<% end %>